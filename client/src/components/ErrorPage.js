const ErrorPage = () => {
    return (
        <div>
            <h1>
                An error has occured. This could be because of an incorrect game ID, or an internal error
            </h1>
            <a href='/'>Return to home</a>
        </div>

    )
}

export default ErrorPage;